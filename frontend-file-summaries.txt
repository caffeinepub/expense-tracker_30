{
  "frontend/src/App.tsx": "Root app component that gates the main UI behind authentication — shows a login screen for unauthenticated users, a profile setup modal for first-time users, and the full tabbed expense tracker (Add Expense, History, Repay Report) for authenticated users with a profile.",
  "frontend/src/index.css": "Global styles with warm saffron/amber OKLCH color system — added `.badge-others` (olive/khaki tone) badge utility class alongside existing category badge classes, plus a `scrollbar-hide` utility for the horizontal chip scroll.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Nunito/Poppins fonts, saffron color scale, custom border radii, card shadows, and slide-up animation.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — added `useGetCallerUserProfile` and `useSaveCallerUserProfile` hooks for authentication/profile management, alongside existing expense CRUD hooks.",
  "frontend/src/lib/categoryUtils.ts": "Utility constants and helpers for category labels, icons, badge CSS classes, sub-categories, and date formatting — updated to include `others` as a full `CategoryKind` with label \"Others\", icon 📦, badge class `badge-others`, and a `buildCategory` case that returns `{ __kind__: 'others', others: null }`.",
  "frontend/src/components/CategoryBadge.tsx": "Color-coded badge component — updated to use `getCategoryKind` to extract the `__kind__` string from the new discriminated-union Category type before looking up labels, icons, and badge classes.",
  "frontend/src/components/AppHeader.tsx": "Sticky app header with rupee icon, app title, INR badge, and a login/logout icon button that uses Internet Identity to authenticate or clear the session.",
  "frontend/src/components/BottomNavigation.tsx": "Fixed bottom tab bar — updated to support three tabs (Add Expense, History, Repay Report) with a RotateCcw icon for the repay tab, active indicator line, and compact labels.",
  "frontend/src/pages/AddExpense.tsx": "Add Expense form page — updated to make the Notes field mandatory when \"Others\" is selected as the category, with a visible hint message, red asterisk label, inline validation error, and form submission blocked until a note is provided.",
  "frontend/src/pages/History.tsx": "Expense history page with date filters, horizontally scrollable category filter chips (All + per-category), per-category summary, and sortable expense list — category chips filter on top of the active date filter, and the grand total/summary dynamically reflects both filters combined.",
  "frontend/index.html": "HTML entry point with mobile viewport meta tags, PWA meta tags, and amber theme color.",
  "frontend/src/pages/RepayReport.tsx": "New page that fetches all expenses, filters for CashTransfer/Repay entries, groups them by person name with amber-highlighted headers, shows per-person subtotals and a grand total, with an empty state when no repay entries exist."
}